# Non-Goals (MVP)

- No replacing GHL as CRM
- No complex patient EMR features
- No multi-location per practice (unless needed later)
- No public directory marketplace features beyond routing
- No overlap routing UI (data model supports it, but UI keeps exclusivity)
